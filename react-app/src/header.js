import './header.css'
import React from 'react';

function Header(props) {
    return <div>
        <h1>To Do List</h1>
        <p>Type your task and click done. Then, mark the item as completed when you finish the task!</p>
    </div>
}

export default Header;